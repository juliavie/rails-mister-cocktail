<h2>The <%= @cocktail.name %></h2>

<h4>Dose/ingredients</h4>
<ul>
  <% @cocktail.doses.each do |dose| %>
  <li> <%= dose.description %>
    <%= dose.ingredient.name %>
  </li>
  <% end %>
</ul>

<h3>Create a new dose</h3>
<%= render "doses/form", cocktail: @cocktail, dose: @dose %>

<%= link_to "Back to list", cocktails_path %>
